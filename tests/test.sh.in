#!/bin/sh
#
# Example shell script wrapper for running "non-trivial" tests
#

cd @CMAKE_CURRENT_BINARY_DIR@
[ ! -d out ] && mkdir out
echo "@CMAKE_CURRENT_BINARY_DIR@ " > out/test.out
./test.bin >> out/test.out

